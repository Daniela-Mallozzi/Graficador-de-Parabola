// build.gradle.kts (nivel de proyecto)
plugins {
    // Plugin de Gradle para Android
    id("com.android.application") version "8.6.0" apply false
    id("org.jetbrains.kotlin.android") version "1.7.10" apply false // Reemplaza con la versión de Kotlin que necesitas
}


dependencies {
    // Aquí se pueden incluir otras dependencias si es necesario
}
